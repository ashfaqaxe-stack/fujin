Hey i want you to crate a shadcn style application like something we can say eject replica of the shadcn
the main features we have different from shadcn will be the compost we wil have will be full fledged production ready components
like as shadcn have basic table right but we will have full fledged data table and it will have whole functionality like pagination, sorting, filtering, searching, and like my main motive is to create something that is already predefined and production ready as for the most tata heavy and saas and dashboard style application we do have to create same style of table same forms and all multiple time so why do we spend time or tokens on creating basic setup as now those thing we can define by default for now like from the day 0 we have a production ready scaffolded application with out production ready components and main part headless components the functionality is already done we just have to change ui if we needed and most of the ui level logic and implementation level logic we have done so that the ai agents will have to only focus on the Business logic and flow the components and there functionality are already defined
and its mainly a full fledged starter template kit with shadcn style rather then installing library direct adding code as per our choice and if we want to make any change in then we can do that as well and cli does the most of things

so we are creating another shadcn yea but our components more complex more ready to use and can be used as it is in production level application and cli does the most of things for adding components and all other things

and will also have the docs exactly like shadcn had so we cna pick its code as its open source the code we cna pick and also follow our production ready way of implementing it as they do have its own custom way of implementation

so for the beginning just setup an application exactly like shadcn but will its "Fujin" and then after that will implement the components and we are not going for much frameworks will so multiple frameworks we will have setup for two framework for now one react and another nextjs (for ui with server actions or mainly server side components )

also the base components will be again from shadcn but like as we are defining our components so we can fill the gaps that shadcn default components have so that we will give the best components out of the box we don't want the same gaps that shadcn does and will be going to cover all production ready components one by one so plan this and do a deep research of the project and for the reference check the shadcn and make sure you follow even better production ready setup in our code and for users end it should be simple and smooth like shadcn cn so scaffold the application go check out the latest library setup and the implementation and production setup we need and keep inm mind the dos will be so simple like shadcn had exactly same and simpl and easy to read then also will setup the well structured and proper mcp of the components just from day so as for each components we have mcp configuration as well so that ai will not do much token spending it can use the mcp and get the well knowledged about the component and can use them as per our need
and plan it in way so that not just a component we will be able to add full entire auth setup like with also like not just being able to add input filed should be able to add an entire form so plan it in that way and then will implement components one by one so start the project do deep research and start it out
